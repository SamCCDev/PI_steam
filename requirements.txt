# Versiones EXACTAS: los modelos .joblib del repo se serializaron con scikit-learn
# 1.8.0 y deben cargarse con la misma versión (aplica en local y en Render).
pandas==3.0.1
numpy==2.4.3
scikit-learn==1.8.0
joblib==1.5.3

# Solo para el ETL (steam_etl.py); el backend del dashboard no lo usa.
requests>=2.31.0
